# wordcount

A small text-statistics library. It exists to give MiniCI something real to
build, test, lint, and containerise.

```bash
pip install -r requirements.txt
PYTHONPATH=src python -m wordcount.cli README.md --top 5
```
